# TASK-FE-055 — Сборка финального index.html

| Поле | Значение |
|------|----------|
| **ID** | TASK-FE-055 |
| **Тип** | frontend |
| **Статус** | TODO |
| **Приоритет** | MEDIUM |
| **Роль** | JS Developer |
| **Дата** | 2026-04-02 |

## Описание

Интегрировать Overview и Assets Gallery в единый `output/index.html`. Обеспечить корректное отображение при всех граничных случаях (fallback, нулевые данные, частичные данные).

## Связанные спецификации

- TASK-FE-054 — HTML-шаблон (зависимость, должен быть выполнен первым)
- `docs/specs/PIPELINE_SPEC.md` §6.3 — отображение ошибок на Dashboard
- `docs/specs/PIPELINE_SPEC.md` §4.3 — итоговая структура `output/`

## Что нужно сделать

1. Проверить корректность шаблона из TASK-FE-054 после прогона `scripts/build-dashboard.sh` (совместно с Java Developer)
2. Проверить отображение во всех граничных случаях из §6.3:
   - `ciStatus: "unknown"` — бейдж серый, текст «Статус неизвестен»
   - `taskBoard.total == 0` — секция показывает «Задачи не найдены» (не пустой экран)
   - `fallback: true` — Assets Gallery показывает placeholder
3. Согласовать с System Analyst финальный вид страницы

## Критерии приёмки

- [ ] `output/index.html` корректно генерируется скриптом `build-dashboard.sh` из шаблона TASK-FE-054
- [ ] Все граничные случаи из §6.3 протестированы вручную
- [ ] Страница открывается по `https://sagat9881.github.io/life_of_t/` после деплоя
- [ ] Нет ошибок в консоли браузера при любых данных

## Метрики

Финальный артефакт Dashboard является точкой видимости всех процессных, технических и игровых метрик проекта.
