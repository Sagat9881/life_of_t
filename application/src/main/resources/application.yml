spring:
  application:
    name: life-of-t
  profiles:
    active: default

server:
  port: 8080

lifegame:
  assets:
    output-dir: ./generated-assets
  browser:
    auto-open: true
    url: http://localhost:8080
