# Assets Module

## 🎨 Назначение

Модуль **assets** содержит **весь визуальный код** игры:
- Процедурная генерация фонов
- Спрайты персонажей
- Анимации
- Иконки
- Все визуальные элементы определены **в коде**

---

## 📁 Структура

```
assets/
├── src/main/
│   ├── typescript/
│   │   ├── backgrounds/          # Процедурные фоны
│   │   │   ├── BackgroundRenderer.ts
│   │   │   ├── RoomBackground.ts
│   │   │   ├── ParkBackground.ts
│   │   │   └── index.ts
│   │   ├── sprites/              # Спрайты (будет)
│   │   ├── animations/           # Анимации (будет)
│   │   └── index.ts
│   └── resources/
│       └── (empty - всё в коде)
├── pom.xml
└── README.md
```

---

## 🎨 Backgrounds

### Подход

Все фоны рисуются **программно** через Canvas API:
- Никаких файлов изображений
- Всё описано попиксельно в коде
- Поддержка динамического освещения
- Изометрическая проекция для Room
- Процедурная генерация деталей

### RoomBackground

**Изометрическая комната:**
- Деревянный пол (тайлы с текстурой)
- Стены (левая + задняя)
- Окно с рамой и стеклом
- Световые лучи из окна (день)
- Комнатное растение
- Динамическое освещение (утро/день/вечер/ночь)

**Цвета:**
- Пол: `#F5F1E8` (кремовый)
- Стены: `#E8DCC8` (бежевый)
- Окно: `#B8D4E8` (голубой)
- Растение: `#7EB09B` (зелёный)

### ParkBackground

**Парк:**
- Небо с градиентом (зависит от времени суток)
- Трава (процедурная текстура из точек)
- Изогнутая дорожка
- Пруд с отражениями
- Деревья (ствол + крона из кругов)
- Звёзды (ночь)

**Адаптивность:**
- Утро: оранжевое небо
- День: голубое небо
- Вечер: розово-оранжевое
- Ночь: тёмное + звёзды

---

## 🔗 Интеграция

### Использование в frontend

```typescript
import { RoomBackground, ParkBackground } from 'assets/backgrounds';

// Создать фон комнаты
const roomBg = new RoomBackground({
  width: 1920,
  height: 1080,
  timeOfDay: 'day'
});

const canvas = roomBg.render();
document.body.appendChild(canvas);

// Или получить data URL
const dataUrl = roomBg.toDataURL();
```

### Изменение времени суток

Просто пересоздать фон с новым `timeOfDay`:

```typescript
const morningBg = new RoomBackground({
  width: 1920,
  height: 1080,
  timeOfDay: 'morning' // 'day' | 'evening' | 'night'
});
```

---

## 📦 Сборка

```bash
mvn clean install
```

TypeScript компилируется в JavaScript и упаковывается в JAR.

---

## 🎯 Философия

**Весь визуал = код**

1. ✅ Попиксельное описание
2. ✅ Процедурная генерация
3. ✅ Динамические эффекты
4. ✅ Никаких статичных изображений
5. ✅ Легко модифицировать
6. ✅ Малый размер бандла

---

## 📝 TODO

- [ ] Sprites: процедурная генерация персонажей
- [ ] Animations: покадровая анимация через код
- [ ] Effects: частицы, погода
- [ ] UI: иконки через SVG path
