/**
 * NPC specification records — immutable data structures loaded from XML.
 * These are pure data with no game logic.
 * The engine layer (domain.npc.engine) uses these specs to create live NPC instances.
 */
package com.sagat9881.lifeoft.domain.npc.spec;
