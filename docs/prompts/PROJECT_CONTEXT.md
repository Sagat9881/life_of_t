# Life of T - Контекст проекта

## Описание проекта

**Life of T** — это симулятор жизни, вдохновлённый игрой Punch Club, разработанный как подарок для Тани. Игра моделирует повседневную жизнь главной героини, её отношения с близкими, управление ресурсами и принятие решений.

### Целевая аудитория
- **Основной игрок**: Таня
- **Платформа**: Telegram Mini App (WebApp)
- **Жанр**: Life Simulation, Visual Novel elements
- **Стиль**: Романтичный, лёгкий, с юмором

---

## Технический стек

### Backend
- **Язык**: Java 21
- **Фреймворк**: Spring Boot 3.2.x
- **Архитектура**: Clean Architecture (Domain, Application, Infrastructure)
- **База данных**: H2 (in-memory для MVP)
- **API**: REST JSON
- **Сборка**: Maven Multi-Module

### Frontend
- **Язык**: TypeScript
- **Фреймворк**: React 18 + Vite
- **Стейт**: Zustand
- **Стили**: CSS Modules + CSS Variables
- **UI**: Custom components (без внешних библиотек)
- **Интеграция**: Telegram WebApp API
- **Сборка**: frontend-maven-plugin

### Deployment
- **Упаковка**: Spring Boot JAR с встроенным frontend
- **Demo**: Desktop executable через jpackage

---

## Архитектура системы

### Backend Modules

```
life-of-t/
├── domain/              # Бизнес-логика и сущности
│   ├── model/          # Игровые сущности (Player, NPC, Pet, Action)
│   ├── service/        # Доменные сервисы
│   └── repository/     # Интерфейсы репозиториев
├── application/         # Сценарии использования
│   ├── usecase/        # Use Cases
│   ├── port/           # Порты (интерфейсы)
│   └── dto/            # DTO для API
├── infrastructure/      # Внешние зависимости
│   ├── persistence/    # JPA репозитории
│   ├── web/            # REST Controllers
│   └── config/         # Конфигурация
├── frontend/            # React приложение
│   ├── src/
│   │   ├── components/ # UI компоненты
│   │   ├── pages/      # Страницы
│   │   ├── store/      # Zustand store
│   │   ├── services/   # API клиенты
│   │   ├── types/      # TypeScript типы
│   │   └── styles/     # CSS стили
│   └── public/         # Статические ресурсы
└── demo/                # Desktop demo приложение
```

### Frontend Structure

```
frontend/src/
├── components/
│   ├── shared/          # Переиспользуемые UI компоненты
│   │   ├── Button/
│   │   ├── Card/
│   │   ├── StatBar/
│   │   ├── LoadingSpinner/
│   │   └── ErrorMessage/
│   ├── game/            # Игровые компоненты
│   │   ├── PlayerPanel/
│   │   ├── ActionCard/
│   │   ├── ActionList/
│   │   ├── NPCCard/
│   │   ├── PetCard/
│   │   ├── RelationshipList/
│   │   ├── TacticCard/
│   │   ├── ConflictResolver/
│   │   ├── ChoiceButton/
│   │   └── EventChoice/
│   └── layout/          # Компоненты разметки
│       ├── AppLayout/
│       └── BottomNav/
├── pages/               # Страницы приложения
│   ├── HomePage.tsx     # Главная (действия, конфликты, события)
│   ├── RelationshipsPage.tsx  # Отношения (NPCs, питомцы)
│   └── ProfilePage.tsx  # Профиль (статистика, достижения)
├── store/
│   └── gameStore.ts     # Zustand store для игрового состояния
├── services/
│   └── api.ts           # HTTP клиент для API
├── types/
│   └── game.ts          # TypeScript типы игровых сущностей
├── utils/
│   ├── constants.ts     # Константы
│   └── formatters.ts    # Утилиты форматирования
└── styles/
    ├── global.css       # Глобальные стили
    ├── variables.css    # CSS переменные
    ├── components/      # Стили компонентов
    └── pages/           # Стили страниц
```

---

## Игровая механика

### Основные сущности

#### Player (Игрок)
- **Stats** (характеристики 0-100):
  - `energy` — Энергия
  - `health` — Здоровье
  - `stress` — Стресс (чем меньше, тем лучше)
  - `mood` — Настроение
  - `money` — Деньги (₽)
  - `selfEsteem` — Самооценка
- **Level** — Уровень развития
- **Name** — Имя (Таня)

#### Time System
- **День** (`day`): текущий игровой день
- **Час** (`hour`): 0-23
- **Временной слот** (`timeSlot`):
  - `MORNING` (6-11): Утро
  - `DAY` (12-17): День
  - `EVENING` (18-22): Вечер
  - `NIGHT` (23-5): Ночь

#### Actions (Действия)
- **Категории**:
  - Работа (дизайн на Tilda)
  - Отдых (сериалы, книги)
  - Отношения (время с мужем, отцом)
  - Хобби (чтение, творчество)
  - Питомцы (игры с Гарфилдом и Сэмом)
  - Базовое (сон, еда)
- **Свойства**:
  - `timeCost` — Время выполнения (часы)
  - `energyCost` — Затраты энергии
  - `effects` — Изменения статов
  - `available` — Доступность

#### NPCs (Персонажи)
- **Муж** (Александр)
- **Отец** (Папа)
- **Relationship** (0-100): уровень отношений

#### Pets (Питомцы)
- **Гарфилд** (кот)
- **Сэм** (цвергпинчер)
- **Stats**:
  - `mood` — Настроение (0-100)
  - `hunger` — Голод (0-100)

#### Conflicts (Конфликты)
- **CSP** (Conflict Stress Points): уровень напряжения
- **Tactics** (тактики разрешения):
  - Извиниться
  - Объяснить
  - Найти компромисс
  - Игнорировать
- **SuccessChance** — Шанс успеха (0-100%)

#### Events (События)
- **Title** — Заголовок
- **Description** — Описание ситуации
- **Choices** (варианты выбора):
  - Текст варианта
  - Последствия (изменения статов, отношений)

---

## API Endpoints

### Game State
```
GET /api/game/state
  → Полное игровое состояние (player, time, actions, npcs, pets, conflicts, events)
```

### Actions
```
POST /api/game/action
  body: { actionCode: string }
  → Выполнить действие, вернуть обновлённое состояние
```

### Conflicts
```
POST /api/game/conflict/tactic
  body: { conflictId: string, tacticCode: string }
  → Применить тактику к конфликту
```

### Events
```
POST /api/game/event/choice
  body: { eventId: string, choiceCode: string }
  → Выбрать вариант действия в событии
```

---

## Визуальный стиль

### Цветовая палитра
```css
--color-primary: #FF6B9D      /* Розовый */
--color-secondary: #C8E6C9    /* Мятный */
--color-accent: #FFE66D       /* Жёлтый */
--color-info: #4FC3F7         /* Голубой */

--color-success: #4CAF50      /* Зелёный */
--color-warning: #FFA726      /* Оранжевый */
--color-danger: #E74C3C       /* Красный */

--color-bg: #FFFFFF
--color-bg-secondary: #F5F5F5
--color-text: #333333
--color-text-secondary: #666666
```

### Типографика
- **Заголовки**: Comfortaa (Google Fonts)
- **Текст**: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif

### UI Принципы
- **Мягкие скругления** (border-radius: 12px)
- **Плавные анимации** (transition: 0.2s ease)
- **Градиенты** для акцентов
- **Haptic feedback** в Telegram
- **Адаптивность** (mobile-first)

---

## Статус разработки (27.02.2026)

### ✅ Готово

#### Backend
- Domain модели (Player, NPC, Pet, Action, Conflict, Event)
- Application Use Cases (ExecuteAction, ResolveConflict, SelectChoice)
- Infrastructure (REST API, H2 database)
- GameSimulationService с временной системой

#### Frontend
- **Shared компоненты**: Button, Card, StatBar, LoadingSpinner, ErrorMessage
- **Layout**: AppLayout, BottomNav
- **Game компоненты**:
  - PlayerPanel (панель игрока)
  - ActionCard, ActionList (действия)
  - NPCCard, PetCard, RelationshipList (отношения)
  - TacticCard, ConflictResolver (конфликты)
  - ChoiceButton, EventChoice (события)
- **Pages**:
  - HomePage (главная с действиями/конфликтами/событиями)
  - RelationshipsPage (отношения)
  - ProfilePage (профиль и статистика)
- **Store**: gameStore (Zustand)
- **Services**: API client
- **Styles**: CSS Variables, все компоненты стилизованы

#### Infrastructure
- Maven multi-module структура
- Frontend интеграция через frontend-maven-plugin
- Demo приложение с desktop launcher
- ComponentTest.tsx для визуальной проверки

### 🔄 В процессе
- Routing (переключение между страницами)
- Интеграция страниц в App.tsx

### 📋 Запланировано
- Сохранение/загрузка игры
- Система достижений
- Telegram WebApp deployment
- Контентное наполнение (больше действий, событий, конфликтов)

---

## Workflow разработки

### Принципы
1. **Итеративность**: маленькие шаги, частые коммиты
2. **Тестирование**: ComponentTest.tsx для визуальной проверки
3. **TypeScript строгость**: no `any`, явная типизация
4. **Clean Code**: компоненты < 200 строк, single responsibility
5. **UX first**: haptic feedback, loading states, error handling

### Git Strategy
- **Main branch**: `main`
- **Feature branch**: `refactor/2026-02-27-core-simulation`
- **Commit style**: «Iteration N: Feature description»

### Build & Run
```bash
# Полная сборка
mvn clean install -DskipTests

# Запуск demo
java -jar demo/target/life-of-t-demo.jar
# или
demo/target/life-of-t-demo.exe

# Frontend development
cd frontend
npm run dev
```

---

## Важные файлы

- `docs/prompts/PROJECT_CONTEXT.md` — этот файл
- `docs/prompts/FRONTEND_SYSTEM_PROMPT.md` — промпт для фронтенда
- `frontend/src/ComponentTest.tsx` — демо всех компонентов
- `frontend/src/store/gameStore.ts` — глобальное состояние игры
- `application/src/main/java/.../GameSimulationService.java` — симуляция игры

---

**Дата обновления**: 27 февраля 2026  
**Версия**: 0.1.0-SNAPSHOT (MVP)  
**Автор**: Александр Захаров
